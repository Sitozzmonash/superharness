# Structured output and tool-call normalization

Set `DEEPSEEK_API_KEY`, then run `python examples/03_structured_and_tools/main.py`. Phase 1 normalizes tool calls but deliberately does not execute them; execution arrives in Phase 2.

