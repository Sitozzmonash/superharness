# Custom tool loop

Set `DEEPSEEK_API_KEY`, then run `python examples/04_custom_tool_loop/main.py`. Pydantic derives the tool schema, validates the provider arguments, executes the function, and returns its result for a final model answer.

