# Built-in tools

Run `python examples/06_builtin_tools/main.py`. It creates a temporary full-access local sandbox and exercises file write/read plus Python subprocess execution without external credentials.

