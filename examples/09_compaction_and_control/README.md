# Compaction and active-turn control

Run `python examples/09_compaction_and_control/main.py`. It compacts an old history prefix into an explicit summary and interrupts a blocking active turn while preserving terminal state.

