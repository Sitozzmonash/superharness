# Phase 6 Plan: Skills and MCP

1. Study pinned Codex Skill parsing/loading and MCP connection, pagination, config, and tool-call behavior.
2. Implement ordered metadata-only Skill discovery, progressive activation, resource confinement, and standard validation.
3. Add staged local and HTTPS Git/GitHub subdirectory installation with immutable revision provenance.
4. Wrap the official MCP Python SDK for stdio and current Streamable HTTP, with tools/resources/prompts, filters, bounded pagination, timeout, cancellation, and capabilities.
5. Add representative 2025 server isolation, common config import, MCPB validation/install, and a replaceable Official MCP Registry adapter.
6. Run local and real external compatibility gates, then complete examples, docs, matrix, packaging, commit, and push.
